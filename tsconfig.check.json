{
  "//": "Chequeo de tipos del codigo fuente. Existe aparte de tsconfig.json porque aquel incluye .next/types, que queda viejo entre builds y tira errores fantasma sobre archivos que ya no existen. Se corre con: npm run typecheck",
  "extends": "./tsconfig.json",
  "include": ["next-env.d.ts", "src/**/*.ts", "src/**/*.tsx", "next.config.ts"],
  "exclude": ["node_modules", ".next"]
}
